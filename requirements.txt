# ULTRON Requirements
# Install with: pip install -r requirements.txt
#
# Notes:
#   - faster-whisper requires CUDA for GPU acceleration
#   - PyQt6 requires Visual C++ Redistributable on Windows
#   - piper-tts models are downloaded automatically on first use
#   - webrtcvad may need: pip install webrtcvad-wheels (Windows)

# ── Core / Async ──────────────────────────────────────────────
httpx==0.27.2                   # Async HTTP client for Ollama API
aiosqlite==0.20.0               # Async SQLite
PyYAML==6.0.2                   # Config file parsing

# ── UI ────────────────────────────────────────────────────────
PyQt6==6.7.1                    # Qt UI framework
qasync==0.27.1                  # Qt + asyncio integration

# ── Speech-to-Text ────────────────────────────────────────────
faster-whisper==1.0.3           # Fast local STT (CUDA-accelerated)
# Alternative: openai-whisper==20231117  (slower but more compatible)

# ── Text-to-Speech ────────────────────────────────────────────
pyttsx3==2.90                   # Offline TTS fallback (system voices)
edge-tts==6.1.12                # Microsoft Edge TTS (online, high quality)
# piper-tts==1.2.0              # Best offline TTS (uncomment when available)

# ── Audio I/O ─────────────────────────────────────────────────
sounddevice==0.5.1              # Cross-platform audio I/O
numpy==2.1.3                    # Audio array processing

# ── Voice Activity Detection ──────────────────────────────────
# webrtcvad==2.0.10             # WebRTC VAD (use webrtcvad-wheels on Windows)
# On Windows, use: pip install webrtcvad-wheels
webrtcvad-wheels==2.0.14        # Pre-built wheels for Windows

# ── System Tools ──────────────────────────────────────────────
psutil==6.1.1                   # CPU/RAM/disk metrics
pyperclip==1.9.0                # Clipboard access
# pycaw==20240210               # Windows volume control (optional)
Pillow==11.0.0                  # Screenshots via PIL

# ── Optional / Enhanced ───────────────────────────────────────
# pvporcupine==3.0.3            # Ultra-low CPU wake word (needs API key)
# resampy==0.4.3                # High-quality audio resampling
# pydub==0.25.1                 # Audio format conversion (for EdgeTTS MP3)
# pyaudio==0.2.14               # Alternative audio I/O

# ── Dev / Testing ─────────────────────────────────────────────
# pytest==8.3.4
# pytest-asyncio==0.24.0
